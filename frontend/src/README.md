# Frontend Structure

## 📁 Структура проекту

```
src/
├── app/                    # Next.js App Router сторінки
│   ├── admin/             # Адмін-панель
│   ├── books/             # Сторінки книг
│   ├── cart/              # Кошик
│   ├── profile/           # Профіль користувача
│   └── layout.tsx         # Головний layout
│
├── components/            # React компоненти
│   ├── admin/             # Адмін компоненти
│   ├── auth/              # Компоненти авторизації
│   ├── books/             # Компоненти книг
│   ├── comments/          # Компоненти коментарів
│   ├── common/            # Загальні компоненти
│   ├── layout/            # Layout компоненти (Header, Footer)
│   └── index.ts           # Barrel export для всіх компонентів
│
├── constants/             # Константи додатку
│   └── index.ts           # API URLs, storage keys, тощо
│
├── context/               # React Context провайдери
│   ├── AuthContext.tsx    # Контекст автентифікації
│   └── ThemeContext.tsx   # Контекст теми
│
├── hooks/                 # Кастомні React хуки
│   ├── useDebounce.ts     # Хук для дебаунсу
│   ├── useLocalStorage.ts # Хук для роботи з localStorage
│   └── index.ts           # Barrel export
│
├── lib/                   # Бібліотеки та конфігурації
│   └── api.ts             # Axios конфігурація
│
├── types/                 # TypeScript типи
│   └── index.ts           # Всі типи додатку
│
└── utils/                 # Утилітарні функції
    └── index.ts           # Форматування, дебаунс, тощо
```

## 🎯 Принципи організації

### 1. **Feature-based структура**
Компоненти згруповані за функціональністю:
- `books/` - все що стосується книг
- `comments/` - все що стосується коментарів
- `auth/` - авторизація та реєстрація

### 2. **Barrel Exports**
Використовуйте `index.ts` файли для зручного імпорту:
```typescript
// Замість
import Header from '@/components/layout/Header';
import Footer from '@/components/layout/Footer';

// Використовуйте
import { Header, Footer } from '@/components';
```

### 3. **Константи**
Всі константи зберігаються в `constants/index.ts`:
```typescript
import { STORAGE_KEYS, API_CONFIG, PAGINATION } from '@/constants';
```

### 4. **Утиліти**
Загальні функції в `utils/index.ts`:
```typescript
import { formatPrice, formatDate, debounce } from '@/utils';
```

### 5. **Хуки**
Кастомні хуки в `hooks/`:
```typescript
import { useDebounce, useLocalStorage } from '@/hooks';
```

## 📝 Правила іменування

- **Компоненти**: PascalCase (`BookCard.tsx`)
- **Хуки**: camelCase з префіксом `use` (`useDebounce.ts`)
- **Утиліти**: camelCase (`formatPrice.ts`)
- **Константи**: UPPER_SNAKE_CASE (`STORAGE_KEYS`)
- **Типи**: PascalCase (`User`, `Book`)

## 🔄 Імпорти

### Правильний порядок імпортів:
1. React та Next.js
2. Сторонні бібліотеки (MUI, тощо)
3. Внутрішні модулі (`@/components`, `@/utils`, тощо)
4. Типи
5. Стилі (якщо є)

```typescript
import { useState, useEffect } from "react";
import { Box, Typography } from "@mui/material";
import { Header, Footer } from "@/components";
import { formatPrice } from "@/utils";
import { Book } from "@/types";
```

## 🎨 Компоненти

### Layout компоненти
- `Header` - навігація та авторизація
- `Footer` - футер сайту

### Book компоненти
- `BookCard` - картка книги
- `BookGrid` - сітка книг
- `SearchFilter` - фільтр пошуку

### Comment компоненти
- `CommentCard` - картка коментаря
- `CommentForm` - форма коментаря
- `CommentSection` - секція коментарів

### Auth компоненти
- `ModalLogin` - модалка входу
- `ModalRegister` - модалка реєстрації

### Common компоненти
- `ConfirmDialog` - діалог підтвердження

## 🛠 Розробка

### Додавання нового компонента:
1. Створіть файл в відповідній папці (`components/books/NewComponent.tsx`)
2. Додайте експорт в `components/index.ts`
3. Використовуйте barrel export для імпорту

### Додавання нової константи:
1. Додайте в `constants/index.ts`
2. Використовуйте через імпорт: `import { NEW_CONSTANT } from '@/constants'`

### Додавання нової утиліти:
1. Додайте в `utils/index.ts`
2. Експортуйте функцію
3. Використовуйте через імпорт: `import { newUtility } from '@/utils'`

